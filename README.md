# notepad-with-voicetyping-feature
A simple speech-to-text web app using the Web Speech API. It listens to your voice in real time, transcribes it into text, and displays it in a styled notepad interface. Offensive words are automatically filtered and replaced with emojis.
